{
  "name": "create-shards-api",
  "dependencies": {
    "@hypecloud/shards": "^0.0.5",
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "build": "parcel build node_modules/@hypecloud/shards/index.js --target node",
    "start": "npm run build && node dist/",
    "develop": "nodemon --exec 'npm run start'"
  },
  "devDependencies": {
    "@babel/core": "^7.7.7",
    "nodemon": "^2.0.2"
  }
}
